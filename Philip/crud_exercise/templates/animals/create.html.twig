{% extends 'base.html.twig' %}
{% block body %}
    <h2 class="text-center">Add New Animal</h2>
    <div class="mx-auto my-2 w-75">
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
    </div>
{% endblock %}